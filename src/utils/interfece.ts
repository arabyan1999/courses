export interface IStore {
    id: number,
    name: string,
    lessonsQuantity: number,
    duration: number,
    backgroundImage: string,
    favorite: boolean,
    newest: boolean,
    popular: boolean
}